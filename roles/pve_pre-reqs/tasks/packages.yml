- name: Install Package Prereqs
  become: true
  ansible.builtin.apt:
    name:
      - curl
      - wget
      - nano
      - python3-full
      - python3-pip
      - python3-venv
      - python3-virtualenv
      - jq
    update_cache: true
